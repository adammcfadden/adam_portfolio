<%= render "layouts/errors", object: @reference %>

<%= bootstrap_form_for(@reference) do |f| %>
  <%= f.text_field :name, placeholder: "Reference Name" %>
  <%= f.hidden_field :user_id, value: current_user.id %>

  <%= f.submit class: "btn-primary btn-block" %>
<% end %>
